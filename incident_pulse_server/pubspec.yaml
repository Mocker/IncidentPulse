name: incident_pulse_server
description: Serverpod 4 backend server for IncidentPulse incident commander and realtime war room.
version: 1.0.0

environment:
  sdk: '^3.3.0'

dependencies:
  serverpod: ^4.0.1
  serverpod_auth_server: ^4.0.1
  http: ^1.2.0
  crypto: ^3.0.7

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
